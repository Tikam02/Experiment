#include<iostream>
using namespace std;

int floorSqrt(int x)
{
	if(x==0 || x==1)
	{
		return x;
	}
	int start=1, end=x, ans;
	while(start <= end)
	{
		if(mid*mid == x)
		return mid;
		
		if(mid*mid < x)
		{
			
		}
	}
}
